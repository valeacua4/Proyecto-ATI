version: "3.8"
services:
  app:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - bd
  bd:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
    - app-volumne:/data/db
  volumes:
      app-volumne: